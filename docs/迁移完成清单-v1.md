# STEM_Agent 迁移完成清单（V1）

更新时间：2026-03-29
执行目录：`D:\科学实验室\3.5-v2\3-5\洗衣机科学实验\STEM_Agent`

## 1. 已迁入的核心功能

- 首页与实验目录：`pages/index/index.vue`
- 科学流程：`pages/experiment/flow.vue` + `components/experiment-flow/steps/*`
- 工程流程：`pages/engineering/flow.vue` + `components/engineering-flow/steps/*`
- 记录中心：`pages/record/record.vue`、`pages/record/detail.vue`
- 成长页：`pages/growth/growth.vue`
- 数据记录页：`pages/data-record/data-record.vue`
- AI 对话：`pages/plan/components/AIChat.vue`

## 2. 配置重构状态

已完成：
- 新增实验独立配置目录：`config/experiments/`
- 新增 11 个实验配置文件（science 3 + engineering 8）
- 新增注册表：
  - `config/registry/experiment-registry.js`
  - `config/registry/category-registry.js`
- 重写兼容入口：
  - `config/science-flow-templates.js`
  - `config/engineering-flow-templates.js`
  - `config/experiment-catalog.js`
- 说明：
  - 历史 `*.legacy.js` 已移除，统一使用独立实验配置

## 3. 页面接线状态

已完成：
- 科学流程页 AI 引导优先读取实验配置 `voiceGuide`
- 工程流程页 AI 引导优先读取实验配置 `voiceGuide`
- 保留 fallback 文案，避免配置缺失导致空白
- 全部实验已切换为独立配置，不再依赖 legacy 模板文件

## 4. 冗余清理结果

已删除（仅 `STEM_Agent` 内）：
- 重复副本目录：
  - `config/config`
  - `utils/utils`
  - `pages/*/*` 重复层
  - `components/*/*` 重复层
  - `static/science-01/science-01`
- 无引用旧配置：
  - `config/experiments.js`
  - `config/lab-config.js`

## 5. 素材目录统一状态

已完成：
- 实验素材目录统一迁移到 `static/experiments/<id>/images|videos`
- 旧目录 `static/science-*` 与 `static/engineering-*` 已迁移并清理
- 批量更新代码与配置中的实验素材路径到新目录
- 各实验 `step1VideoUrl` 已统一到 `/static/experiments/<id>/videos/intro.mp4`

## 6. 当前保留的兼容文件


说明：
- 当前已不再保留 legacy 兼容模板文件。
- 兼容层仅保留在 `science-flow-templates.js` / `engineering-flow-templates.js` 的读取入口。

## 7. 待后续迭代（建议）

- 增加自动校验脚本：检查每个实验 `voiceGuide` 与素材路径完整性

